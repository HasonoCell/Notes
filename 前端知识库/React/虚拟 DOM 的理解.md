虚拟 DOM 相当于一种在运行时做出妥协的方案，通过运行时增加额外的开销，Diff 算法比对新旧虚拟 DOM 树，从而实现非命令式地，最小化地更新真实 DOM，同时也为跨端方案提供了可能性。



而如今，随着项目体量的增大，虚拟 DOM 的运行时开销不可再被忽略，随着现代编译技术的发展，我们不用再通过运行时 Diff 性能消耗极大的虚拟 DOM 树去更新真实 DOM，而是可以在编译期就确定需要更新的 UI 部分，然后精确地为那一部分生成命令式的原生 JS 代码，从而实现了极小的性能开销（比如 Svelte）

